apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-provider
data:
  dashboards.yaml: |
    apiVersion: 1
    providers:
      - name: 'Guardian Pi'
        folder: 'Guardian Pi'
        type: file
        options:
          path: /var/lib/grafana/dashboards
